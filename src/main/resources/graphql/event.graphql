extend type Query {
    replay (
        topic: String!
        aggregateId: String!
        offset: BigInteger
        limit: Int
    ): [Event]
}

type Event {
    id: ID
    topic: String
    type: String
    aggregateId: String
    data: JSON
    metadata: JSON
    created: String
}