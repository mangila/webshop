extend type Query {
    replay (input: EventQueryReplay): [Event]
}

input EventQueryReplay {
    topic: String!
    aggregateId: String!
    offset: BigInteger
    limit: Int
}

type Event {
    id: ID
    topic: String
    type: String
    aggregateId: String
    data: JSON
    created: String
}