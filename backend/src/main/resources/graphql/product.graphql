extend type Query {
    findProductById(input: ProductByIdQuery):Product
}

input ProductByIdQuery {
    id: ID!
}

type Product {
    id: ID
    name: String
    price: Float
    created: String
    updated: String
    attributes: JSON
}