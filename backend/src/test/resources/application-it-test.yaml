# Spring Properties
spring:
  boot:
    admin:
      client:
        enabled: false
# Logging properties
logging:
  file:
    name:
    path:
# SpringDoc
springdoc:
  api-docs:
    enabled: false
# Actuator
management:
  tracing:
    enabled: false
  zipkin:
    tracing:
      export:
        enabled: false
jdbc:
  datasource-proxy:
    enabled: false