extend type Query {
    findProductById(input: ProductIdQuery):Product
}

input ProductIdQuery {
    value: ID!
}

type Product {
    id: ID
    name: String
    price: Money
    created: String
    updated: String
    attributes: JSON
}